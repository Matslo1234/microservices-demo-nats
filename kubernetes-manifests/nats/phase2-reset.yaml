# On-demand destructive reset of rebuildable Phase 2 projection KV state.
apiVersion: batch/v1
kind: Job
metadata:
  name: nats-phase2-projection-reset
  namespace: nats
  labels:
    app.kubernetes.io/name: nats-phase1-verification
spec:
  backoffLimit: 0
  ttlSecondsAfterFinished: 3600
  template:
    metadata:
      labels:
        app.kubernetes.io/name: nats-phase1-verification
    spec:
      restartPolicy: Never
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
      containers:
      - name: reset
        image: natsio/nats-box:0.19.7-nonroot
        command:
        - sh
        - -ec
        - |
          args="--server=${NATS_URL} --user=${NATS_USER} --password=${NATS_PASSWORD} --tlsca=/etc/nats-ca/ca.crt"
          nats ${args} kv del "${STOREFRONT_PRODUCTS_BUCKET}" --force
          nats ${args} kv del "${STOREFRONT_CARTS_BUCKET}" --force
          nats ${args} kv del RECOMMENDATION_CATALOG --force
          nats ${args} kv add "${STOREFRONT_PRODUCTS_BUCKET}" --history=5 --replicas=3 --storage=file --ttl=0s --max-bucket-size=536870912 --placement="${NATS_CLUSTER_NAME}"
          nats ${args} kv add "${STOREFRONT_CARTS_BUCKET}" --history=2 --replicas=3 --storage=file --ttl=0s --max-bucket-size=536870912 --placement="${NATS_CLUSTER_NAME}"
          nats ${args} kv add RECOMMENDATION_CATALOG --history=5 --replicas=3 --storage=file --ttl=0s --max-bucket-size=268435456 --placement="${GLOBAL_ASSET_CLUSTER}"
          nats ${args} consumer rm "${STOREFRONT_EVENT_STREAM}" "${STOREFRONT_PROJECTION_DURABLE}" --force || true
          nats ${args} consumer rm "${STOREFRONT_EVENT_STREAM}" recommendation-catalog-v1 --force || true
        envFrom:
        - configMapRef:
            name: nats-region-config
        - secretRef:
            name: nats-admin-credentials
        volumeMounts:
        - name: ca
          mountPath: /etc/nats-ca
          readOnly: true
        resources:
          requests:
            cpu: 20m
            memory: 32Mi
          limits:
            cpu: 100m
            memory: 128Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop: ["ALL"]
          readOnlyRootFilesystem: true
      volumes:
      - name: ca
        configMap:
          name: nats-ca
